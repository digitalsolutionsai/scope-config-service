{
  "name": "@dsai/scopeconfig",
  "version": "1.0.0",
  "description": "TypeScript SDK for ScopeConfig gRPC service",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "generate": "npx buf generate",
    "lint": "eslint src --ext .ts",
    "test": "jest"
  },
  "keywords": [
    "scopeconfig",
    "grpc",
    "config",
    "configuration"
  ],
  "author": "DSAI",
  "license": "MIT",
  "dependencies": {
    "@grpc/grpc-js": "^1.9.0",
    "@grpc/proto-loader": "^0.7.0"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.57.2",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "src",
    "proto"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
